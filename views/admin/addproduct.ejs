<%- include('../partials/admin/header') -%>
    <section class="content-main">
        <form action="/admin/productpublish" method="post" enctype="multipart/form-data">
            <div class="row">
                <div class="col-9">
                    <div class="content-header">
                        <h2 class="content-title">Add New Product</h2>
                        <div>
                            <!-- <button class="btn btn-light rounded font-sm mr-5 text-body hover-up">Save to draft</button> -->
                            <button class="btn btn-md rounded font-sm hover-up">Publish</button>
                        </div>
                    </div>
                </div>
                <div class="col-lg-6">


                    <div class="card mb-4">
                        <div class="card-header">
                            <h4>Basic</h4>
                        </div>
                        <div class="card-body">

                            <div class="mb-4">
                                <label for="product_name" class="form-label">Product title</label>
                                <input type="text" placeholder="Type here" class="form-control" name="productname"
                                    id="product_name" required>
                            </div>
                            <div class="mb-4">
                                <label class="form-label">Full description</label>
                                <textarea placeholder="Type here" class="form-control" name="productdescription"
                                    rows="4" required></textarea>
                            </div>
                            <div class="row">
                                <div class="col-lg-4">
                                    <div class="mb-4">
                                        <label class="form-label">price</label>
                                        <div class="row gx-2">
                                            <input placeholder="₹" type="text" name="productprice" class="form-control" required>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-lg-4">
                                    <div class="mb-4">
                                        <label class="form-label">Stock Quantity</label>
                                        <input placeholder="₹" type="text" name="productquantity" class="form-control" required>
                                    </div>
                                </div>
                               
                            </div>
                        </div>
                    </div> <!-- card end// -->
                   
                </div>
                <div class="col-lg-3">
                    
<!-- old -->
                    <div class="card mb-4">
                        <div class="card-header">
                            <h4>Media</h4>
                        </div>
                        <div class="card-body">
                            <div class="input-upload">
                                <div class="image-preview-container">
                                    <img id="image-preview-1" class="image-preview" src="" alt="">
                                </div>
                                <div class="image-preview-container">
                                    <img id="image-preview-2" class="image-preview" src="" alt="">
                                </div>
                                <div class="image-preview-container">
                                    <img id="image-preview-3" class="image-preview" src="" alt="">
                                </div>
                                <div class="image-preview-container">
                                    <img id="image-preview-4" class="image-preview" src="" alt="">
                                </div>
                                <input id="file-input" class="form-control" name="files" type="file" required multiple>
                            </div>
                        </div>
                    </div>


<!-- 

                    <div class="card mb-4">
                        <div class="card-header">
                            <h4>Photos</h4>
                        </div>
                        <div class="card-body">
                            <div class="image-preview">
                                <img src="asset/imgs/theme/upload.svg" alt="">
                                <input class="form-control" type="file" name="productimage" multiple>
                            </div>
                        </div>
                    </div> card end// -->
                    

                    <!-- <div class="card mb-4">
                        <div class="card-header">
                          <h4>Media</h4>
                        </div>
                        <div class="card-body">
                          <div class="input-upload">
                            <div class="image-preview-container">
                              <img id="image-preview-1" class="image-preview" src="" alt="">
                              <button type="button" class="remove-button" data-id="1">Remove</button>
                            </div>
                            <div class="image-preview-container">
                              <img id="image-preview-2" class="image-preview" src="" alt="">
                              <button type="button" class="remove-button" data-id="2">Remove</button>
                            </div>
                            <div class="image-preview-container">
                              <img id="image-preview-3" class="image-preview" src="" alt="">
                              <button type="button" class="remove-button" data-id="3">Remove</button>
                            </div>
                            <div class="image-preview-container">
                              <img id="image-preview-4" class="image-preview" src="" alt="">
                              <button type="button" class="remove-button" data-id="4">Remove</button>
                            </div>
                            <input id="file-input" class="form-control" name="files" type="file" required multiple>
                          </div>
                        </div>
                      </div>
                      <button type="submit">Publish</button> -->

                    
                  

                      




                    <div class="card mb-4">
                        <div class="card-header">
                            <h4>Organization</h4>
                        </div>
                        <div class="card-body">
                            <div class="row gx-2">
                                <div class="col-sm-6 mb-3">
                                    <label class="form-label">Category</label>
                                    <select name="productcategory" class="form-select">
                                        <% categories.map((items)=>{ %>
                                            <option>
                                                <%= items.categoryName %>
                                            </option>
                                            <% }) %>
                                    </select>
                                </div>
                                <div class="col-sm-6 mb-3">
                                    <label class="form-label">Brand</label>
                                    <select name="productbrand" class="form-select">
                                        <option>Royal Canin </option>
                                        <option> Whiskas </option>
                                        <option> Arden Grange </option>
                                        <option>zupreem</option>
                                        <option>Acana</option>
                                        <option>Bites</option>
                                        <!-- <% categories.map((items)=>{ %>
                                            <option>
                                                <%= items.productbrand %>
                                            </option>
                                        <% }) %> -->
                                    </select>
                                </div>                             
                            </div> <!-- row.// -->
                        </div>
                    </div> <!-- card end// -->
                </div>
            </div>
        </form>
    </section> <!-- content-main end// -->
  

    <%- include('../partials/admin/footer') -%>



<!-- 
<script>
  // Get the file input element and image preview elements
const fileInput = document.getElementById('file-input');
const imagePreviews = document.querySelectorAll('.image-preview');
const removeButtons = document.querySelectorAll('.remove-button');

// Add an event listener to the file input for the change event
fileInput.addEventListener('change', function() {
  // Check if any files are selected
  if (fileInput.files && fileInput.files.length > 0) {
    // Loop through each selected file
    for (let i = 0; i < fileInput.files.length; i++) {
      const file = fileInput.files[i];
      // Create a FileReader object
      const reader = new FileReader();

      // Set up the FileReader onload event
      reader.onload = function(e) {
        // Set the src attribute of the corresponding image preview to the data URL
        const emptyPosition = getEmptyPosition();
        if (emptyPosition !== -1) {
          imagePreviews[emptyPosition].src = e.target.result;
          removeButtons[emptyPosition].classList.remove('removed');
        }
      };

      // Read the selected file as a data URL
      reader.readAsDataURL(file);
    }
  }
});

// Add event listeners to remove buttons for individual image removal
removeButtons.forEach((button, index) => {
  button.addEventListener('click', function() {
    const id = button.getAttribute('data-id');
    const imagePreview = document.getElementById(`image-preview-${id}`);
    
    // Clear the corresponding image preview
    imagePreview.src = '';
    button.classList.add('removed');
  });
});

// Get the position of the first empty image
function getEmptyPosition() {
  for (let i = 0; i < imagePreviews.length; i++) {
    if (imagePreviews[i].src === '' || imagePreviews[i].src === 'undefined') {
      return i;
    }
  }
  return -1;
}


</script> -->










    


    
<!-- old -->
    <script>
        // Get the file input element and image preview elements
        const fileInput = document.getElementById('file-input');
        const imagePreviews = document.querySelectorAll('.image-preview');
    
        // Add an event listener to the file input for the change event
        fileInput.addEventListener('change', function() {
            // Check if any files are selected
            if (fileInput.files && fileInput.files.length > 0) {
                // Loop through each selected file
                for (let i = 0; i < fileInput.files.length; i++) {
                    const file = fileInput.files[i];
                    // Create a FileReader object
                    const reader = new FileReader();
    
                    // Set up the FileReader onload event
                    reader.onload = function(e) {
                        // Set the src attribute of the corresponding image preview to the data URL
                        imagePreviews[i].src = e.target.result;
                    };
    
                    // Read the selected file as a data URL
                    reader.readAsDataURL(file);
                }
            }
        });
    </script>




